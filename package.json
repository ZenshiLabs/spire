{
  "name": "spire",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=web",
    "dev:web": "turbo run dev --filter=web",
    "dev:cli": "turbo run build --filter=@spire/types && pnpm --filter=@zenshilabs/spire dev",
    "lint": "turbo run lint --ui=stream",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "pnpm --filter @spire/db db:generate",
    "db:migrate": "pnpm --filter @spire/db db:migrate",
    "db:push": "pnpm --filter @spire/db db:push"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.16",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
